-- ==============================================
-- KEYZORXZ's MUSCLE MASTER EXCLUSIVE HUB
-- ==============================================

local OWNER_USERNAME = "keyler632"
local OWNER_USER_ID = 2043745142
local IS_OWNER = game.Players.LocalPlayer.Name == OWNER_USERNAME or game.Players.LocalPlayer.UserId == OWNER_USER_ID

if not IS_OWNER then
    game.Players.LocalPlayer:Kick("No autorizado")
    return
end

-- LIB
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/bananaGanteng/Legend/refs/heads/main/BedahNih.lua"))()

local Window = OrionLib:MakeWindow({
    Name = "KM MAIN",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "KMHub"
})

-- TABS
local FarmTab = Window:MakeTab({
    Name = "Farm",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local PetsTab = Window:MakeTab({
    Name = "Pets",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local player = game.Players.LocalPlayer
FarmTab:AddParagraph("KM Hub", "Welcome, " .. player.Name)

-- STATES
local T = {
    AntiKick = false,
    AntiPenalty = false,
    AutoGlitch = false,
    AutoRebirth = false,
    AutoEquip = false,
    AutoPets = false,
    AutoKing = false,
    AutoFarm = false,
    AutoDupe = false
}

-- ================= FUNCTIONS =================

local function AdvancedAntiKick()
    if T.AntiKick then return end
    T.AntiKick = true

    local PlayersService = game:GetService("Players")
    local LocalPlayer = PlayersService.LocalPlayer

    hookfunction(LocalPlayer.Kick, function() return nil end)
    hookfunction(PlayersService.Kick, function(target)
        if target == LocalPlayer then return nil end
    end)
    hookfunction(game:GetService("TeleportService").Teleport, function() return nil end)
end

local function AntiPenalty()
    if T.AntiPenalty then return end
    T.AntiPenalty = true

    hookfunction(game:GetService("Players").Ban, function() return end)

    game:GetService("RunService").Heartbeat:Connect(function()
        if not T.AntiPenalty then return end
        local char = player.Character
        if char and char:FindFirstChild("Humanoid") then
            local h = char.Humanoid
            h.WalkSpeed = 16
            h.JumpPower = 50
        end
    end)
end

local function AutoGlitch()
    task.spawn(function()
        while T.AutoGlitch do
            local r = game.ReplicatedStorage:FindFirstChild("GlitchPower", true)
            if r then r:FireServer(math.huge) end
            wait(0.1)
        end
    end)
end

local function AutoRebirth()
    task.spawn(function()
        while T.AutoRebirth do
            local Power = player.leaderstats:FindFirstChild("Power", true)
            local Btn = player.PlayerGui:FindFirstChild("RebirthButton", true)

            if Power and Btn and Power.Value >= 1e12 then
                if Btn:IsA("TextButton") then
                    fireclickdetector(Btn.ClickDetector)
                else
                    Btn:FireServer()
                end
            end
            wait(0.5)
        end
    end)
end

local function AutoEquipment()
    task.spawn(function()
        while T.AutoEquip do
            local Inv = player:FindFirstChild("Inventory", true)
            local Remote = game.ReplicatedStorage:FindFirstChild("EquipGear", true)

            if Inv and Remote then
                local Best = {}
                for _, g in pairs(Inv:GetChildren()) do
                    local s = g:FindFirstChild("PowerStat", true)
                    if s then table.insert(Best, {g.Name, s.Value}) end
                end
                table.sort(Best, function(a,b) return a[2] > b[2] end)

                for i=1,5 do
                    if Best[i] then
                        Remote:FireServer(Best[i][1])
                        wait(0.2)
                    end
                end
            end
            wait(300)
        end
    end)
end

local function AutoPets()
    task.spawn(function()
        while T.AutoPets do
            local Pets = player:FindFirstChild("Pets", true)
            local Summon = game.ReplicatedStorage:FindFirstChild("SummonPet", true)
            local Equip = game.ReplicatedStorage:FindFirstChild("EquipPet", true)

            if Pets and Summon and Equip then
                for _, p in pairs(Pets:GetChildren()) do
                    Summon:FireServer(p.Name)
                    Equip:FireServer(p.Name)
                    wait(0.3)
                end
            end
            wait(600)
        end
    end)
end

local function AutoDupePets()
    task.spawn(function()
        while T.AutoDupe do
            local Remote = game.ReplicatedStorage:FindFirstChild("DupePet", true)
            if Remote then
                Remote:FireServer("Titan Muscle Pet")
            end
            wait(3)
        end
    end)
end

local function AutoKing()
    task.spawn(function()
        while T.AutoKing do
            local Area = workspace:FindFirstChild("KingArea", true)
            local Claim = game.ReplicatedStorage:FindFirstChild("ClaimKing", true)

            if Area and Claim then
                Claim:FireServer()
            end
            wait(2)
        end
    end)
end

local function AutoFarm()
    task.spawn(function()
        while T.AutoFarm do
            local Attack = game.ReplicatedStorage:FindFirstChild("AttackObject", true)
            if Attack then
                for _, obj in pairs(workspace:GetDescendants()) do
                    if obj:FindFirstChild("Health") then
                        Attack:FireServer(obj)
                    end
                end
            end
            wait(1)
        end
    end)
end

-- ================= FARM TAB =================

FarmTab:AddToggle({
    Name = "Anti Kick",
    Callback = function(v)
        if v then AdvancedAntiKick() end
    end
})

FarmTab:AddToggle({
    Name = "Anti Penalty",
    Callback = function(v)
        T.AntiPenalty = v
        if v then AntiPenalty() end
    end
})

FarmTab:AddToggle({
    Name = "Auto Glitch",
    Callback = function(v)
        T.AutoGlitch = v
        if v then AutoGlitch() end
    end
})

FarmTab:AddToggle({
    Name = "Auto Rebirth",
    Callback = function(v)
        T.AutoRebirth = v
        if v then AutoRebirth() end
    end
})

FarmTab:AddToggle({
    Name = "Auto King",
    Callback = function(v)
        T.AutoKing = v
        if v then AutoKing() end
    end
})

FarmTab:AddToggle({
    Name = "Auto Farm",
    Callback = function(v)
        T.AutoFarm = v
        if v then AutoFarm() end
    end
})

-- ================= PETS TAB =================

PetsTab:AddToggle({
    Name = "Auto Equip",
    Callback = function(v)
        T.AutoEquip = v
        if v then AutoEquipment() end
    end
})

PetsTab:AddToggle({
    Name = "Auto Pets",
    Callback = function(v)
        T.AutoPets = v
        if v then AutoPets() end
    end
})

PetsTab:AddToggle({
    Name = "Auto Dupe Pets",
    Callback = function(v)
        T.AutoDupe = v
        if v then AutoDupePets() end
    end
})
